version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
  - name: TestJob
    task:
      prologue:
        commands:
          - checkout
      jobs:
        - name: Robot Test
          commands:
            - python3 -m robot -d Results --name SuiteName TestCases/*.robot
